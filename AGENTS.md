Start every reply with "Yes Master?"

We're going to do a multi crypto trading bot project.

We'll use pure MLX in the hot path.

Minimal: less code possible (files should be max 400-500 lines long).

Start every python process with ".venv/bin/python"